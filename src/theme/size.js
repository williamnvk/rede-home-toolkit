// @flow
import base from './base';

export const half: string = `${base / 2}px`;
export const normal: string = `${base}px`;
export const large: string = `${base * 2}px`;
export const extraLarge: string = `${base * 4}px`;
