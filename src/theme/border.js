// @flow
import base from './base';
import measurer from './measurer';

export const width: string = `${base / 10}px`;
export const color: string = 'rgba(0,0,0,0.2)';
export const radius: string = `${base / 4}${measurer}`;
