import { StyleSheet } from 'react-native';
import base from './base';

export const width = `${parseFloat(StyleSheet.hairlineWidth)}px`;
export const color = 'rgba(0,0,0,0.2)';
export const radius = `${base / 4}px`;
