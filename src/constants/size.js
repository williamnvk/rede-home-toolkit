// @flow
import { DEFAULT_SIZE } from './constants';

export const gapDefault: string = `${DEFAULT_SIZE * 2}px`;
export const gapLarge: string = `${DEFAULT_SIZE * 4}px`;
export const sizeHalf: string = `${DEFAULT_SIZE / 2}px`;
export const sizeDefault: string = `${DEFAULT_SIZE}px`;
export const sizeLarge: string = `${DEFAULT_SIZE * 2}px`;
export const sizeLarger: string = `${DEFAULT_SIZE * 3}px`;
export const sizeExtraLarge: string = `${DEFAULT_SIZE * 4}px`;
