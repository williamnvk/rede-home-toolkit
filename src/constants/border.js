// @flow
import { DEFAULT_RADIUS_SIZE } from './constants';

export const borderWidth: number = 1;
export const borderWidthLarge: number = borderWidth * 2;
export const borderWidthExtra: number = borderWidth * 2.5;
export const borderColor: string = 'rgba(0,0,0,0.2)';
export const borderRadius: string = `${DEFAULT_RADIUS_SIZE / 2}px`;
export const borderRadiusLarge: string = `${DEFAULT_RADIUS_SIZE *
  2}px`;
export const borderRadiusExtraLarge: string = `${DEFAULT_RADIUS_SIZE *
  4}px`;
