export type FontSizeProps = {
  tiny?: boolean,
  small?: boolean,
  large?: boolean,
  extra?: boolean,
};

export type FontFamilyProps = {
  light?: boolean,
  medium?: boolean,
  bold?: boolean,
  black?: boolean,
  heavy?: boolean,
};
